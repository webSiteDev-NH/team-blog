<div class="row">
  <div class="col-md-6 col-md-offset-3 text-center">
    <h2>チーム一覧</h2>
    <div class="teams-list">
      <% @teams.each do |team| %>
        <p><%=link_to team.team_name, team_path(team) %></p>
      <% end %>
    </div>
  </div>
</div>